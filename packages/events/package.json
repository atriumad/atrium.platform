{
  "name": "@atrium/events",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'events: no build needed'",
    "test": "bun test",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atrium/shared": "workspace:*",
    "@atrium/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
