Clean text field with optional label + hint — for forms and contact surfaces on light backgrounds.

```jsx
<Input label="Email" type="email" placeholder="you@studio.com" />
<Input label="Budget" hint="Required" invalid />
```

Border deepens to teal on focus with an amber ring. `invalid` switches to amber. Spreads native input props (placeholder, value, onChange, …).
