Small flat status/category tag (non-interactive) — for labels like "New", "Featured", post categories.

```jsx
<Badge tone="amber">Featured</Badge>
<Badge tone="outline">SEO</Badge>
```

`tone`: `mint | teal | amber | cloud | outline`. For interactive/selectable pills use `Chip` instead.
